enable
configure terminal
!
hostname {{ hostname }}
no ip domain-lookup
line console 0
exec-timeout 0 0
logging synchronous
!
{% for interface in interfaces -%}
interface {{ interface.name }}
ip address {{ interface.address }} {{ interface.mask }}
no shutdown
exit
{% endfor -%}
!
end
write
